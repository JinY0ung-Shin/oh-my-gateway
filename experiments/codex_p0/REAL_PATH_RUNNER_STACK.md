# Stacked PR note

This P0a-1 runner branch is intentionally based on `codex-p0-ownership-probe` / PR #165 so the review diff contains only real-path conformance work. After #165 lands, retarget the P0a pull request to `main`.

Canonical spike: #163. Architecture history: #162.
